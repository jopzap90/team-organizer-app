# PostgreSQL (required for both local and Vercel)
# Local: use Neon (free), Vercel Postgres, or Docker.
# Vercel: set in Project → Settings → Environment Variables (or use Vercel Postgres integration).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"